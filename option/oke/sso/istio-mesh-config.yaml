apiVersion: v1
kind: ConfigMap
metadata:
  name: istio
  namespace: istio-system
data:
  # Istio accepts a partial MeshConfig. oauth2-proxy is added idempotently by
  # merge_istio_sso.py after this baseline is created.
  mesh: |
