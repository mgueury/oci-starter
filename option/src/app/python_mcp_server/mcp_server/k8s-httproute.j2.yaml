apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ##K8S_APP_PREFIX##-mcp-server-route
spec:
  parentRefs:
  - name: oke-gateway
    namespace: gateway    
{%- if tls == "new_http_01" %}
  hostnames:
  - "##TF_VAR_dns_name##"
{%- endif %}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /##K8S_APP_PREFIX##/mcp_server
    filters:
    - type: URLRewrite
      urlRewrite:
        path:
          type: ReplacePrefixMatch
          replacePrefixMatch: /
    backendRefs:
    - name: ##K8S_APP_PREFIX##-mcp-server-service
      port: 2025