apiVersion: apps/v1
kind: Deployment
metadata:
  name: ##K8S_APP_PREFIX-mcp-server-dep
  labels:
      app: ##K8S_APP_PREFIX##-mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ##K8S_APP_PREFIX##-mcp-server
  template:
    metadata:
      labels:
        app: ##K8S_APP_PREFIX##-mcp-server
    spec:
      containers:
      - name: app
        image: ##DOCKER_PREFIX##/##K8S_APP_PREFIX##-mcp_server:##DOCKER_IMG_VERSION##
        ports:
        - containerPort: 2025
          name: app-port
        env:
        - name: DB_URL
          valueFrom:
            secretKeyRef:
              name: ##K8S_APP_PREFIX##-db-secret
              key: db_url
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: ##K8S_APP_PREFIX##-db-secret
              key: db_user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ##K8S_APP_PREFIX##-db-secret
              key: db_password
#        - name: POD_IP
#          valueFrom:
#            fieldRef:
#              fieldPath: status.podIP
{%- if db_type == "nosql" %}
        - name: TF_VAR_compartment_ocid
          valueFrom:
            secretKeyRef:
              name: ##K8S_APP_PREFIX##-db-secret
              key: TF_VAR_compartment_ocid
        - name: TF_VAR_nosql_endpoint
          valueFrom:
            secretKeyRef:
              name: ##K8S_APP_PREFIX##-db-secret
              key: TF_VAR_nosql_endpoint
{%- endif %}
      imagePullSecrets:
      - name: ocirsecret        
---
apiVersion: v1
kind: Service
metadata:
  name: ##K8S_APP_PREFIX-mcp-server-service
spec:
  selector:
    app: ##K8S_APP_PREFIX##-mcp-server
  ports:
  - name: http-service-port
    protocol: TCP
    port: 2025
    targetPort: app-port
---